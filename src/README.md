# src/

Firmware source will live here once the nRF52 toolchain (nRF5 SDK vs. nRF
Connect SDK/Zephyr vs. upstream Zephyr) is chosen — see the repo root
README's Status section. Empty placeholder until then.
